{
  "name": "autotest-portfolio",
  "version": "1.0.0",
  "description": "Personal Playwright automation portfolio example — Page Object Model pattern demo",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
